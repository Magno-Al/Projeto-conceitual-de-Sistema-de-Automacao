PySide6>=6.5
# A API do datastore/servidor do pymodbus mudou bastante na série 3.8+.
# Fixamos a faixa com a API clássica (ModbusSlaveContext, zero_mode,
# StartTcpServer(context, address)) usada pelo mock_plc.py.
pymodbus>=3.5,<3.8
